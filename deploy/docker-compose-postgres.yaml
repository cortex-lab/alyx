services:
  postgres:
    image: postgres:17
    container_name: alyx_postgres
    env_file: ../alyx/alyx/.env_local
    volumes:
      - alyx_postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  alyx_postgres:
